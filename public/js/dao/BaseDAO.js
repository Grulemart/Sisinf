class BaseDAO {

    constructor(db) {
        this.db = db; // Debes proporcionar la instancia de la base de datos
      }

    async insertar(data) {
        // Implementación
    }

    async actualizar(data) {
        // Implementación
    }

    async eliminar(id) {
        // Implementación
    }

    async obtenerPorId(id) {
        // Implementación
    }

    async obtenerTodos() {
        // Implementación
    }
}
